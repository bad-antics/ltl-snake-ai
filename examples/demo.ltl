// examples/demo.ltl — runnable demo for ltl_snake_ai

import std::io
use ltl_snake_ai as lib

fn main() {
    io::println("ltl_snake_ai demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}